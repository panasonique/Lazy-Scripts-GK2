# 🛠 Lazy Scripts v3.4.0 (for Blender 4.x)

Динамический аддон для Blender, который строит интерфейс напрямую из структуры ваших папок и исполняемых Python-скриптов. Больше не нужно прописывать каждую кнопку в коде — просто закиньте скрипт в папку.

## 🌟 Основные фишки
- **Авто-генерация UI**: Панели, подпанели и кнопки создаются автоматически на основе иерархии папок.
- **Умные переменные**: Поддержка Float, Int и Custom полей через словарь `info` внутри скрипта.
- **Динамические заголовки**: Вывод значений переменных прямо в название панели с помощью фигурных скобок `{id}`.
- **Горячие клавиши**: Настройка шорткатов прямо в скрипте (напр. `Ctrl Shift A`).

## 📂 Структура папок и логика именования
Аддон сортирует элементы по цифровому префиксу и очищает имена для UI.

### Пример структуры:
- `20_GK2_Tools/` — Главная панель в интерфейсе.
- `40_Step_#1 {move_step_1}/` — Подпанель. Благодаря `{move_step_1}` в заголовке будет отображаться текущее значение этой переменной.
- `42_Input_Field.py` — Скрипт, который отрисует поле ввода в этой панели.

## 📝 Как создать свой скрипт
В начале каждого `.py` файла должен быть словарь `info`. Пример для поля ввода:

```python
info = {
    "name": "Step Value",
    "type": "input_custom",  # Типы: button, int, input_float, input_custom, text
    "prop_id": "move_step_1",
    "default": 1.0,
    "min": 0.0,
    "max": 100.0,
    "description": "Описание при наведении на HELP"
}

import bpy
# Логика вашего скрипта...
